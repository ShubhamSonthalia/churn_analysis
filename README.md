# Customer Churn Analysis & Prevention

A Dash web app that predicts customer churn with a trained neural network
and turns that prediction into a concrete retention playbook — not just
a number.

## Quick start

```bash
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:8050. No database setup required.

